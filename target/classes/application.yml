server:
  port: ${PORT:8080}

meal-gpt:
  ollama:
    base-url: ${OLLAMA_BASE_URL:http://127.0.0.1:11434}
    model: ${OLLAMA_MODEL:llava:latest}
  spoonacular:
    api-key: ${SPOONACULAR_API_KEY:}
  upload-dir: ${UPLOAD_DIR:uploads}
  openclaw:
    shared-secret: ${OPENCLAW_SHARED_SECRET:}
